# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Local lima/test env scratch
.test-out/
*.log

# Generated by build-index.sh on releases — only the tagged version is the source of truth.
# We DO commit index.json on release commits, but local-run builds shouldn't pollute working tree.
index.json.tmp
