name: adminer
services:
  adminer:
    image: adminer:5.4.1
    deploy:
      resources:
        reservations:
          memory: 32M
    restart: unless-stopped
    ports:
    - 8080:8080
version: '3.7'
x-powerlab:
  store_app_id: adminer
  title: Adminer
  tagline: Adminer
  category: System
  port_map:
  - container: 8080
    host: 8080
    protocol: http
  source:
    catalog: casaos-appstore
    upstream_id: Adminer
    upstream_repo: https://github.com/IceWhaleTech/CasaOS-AppStore.git
    upstream_path: Apps/Adminer/docker-compose.yml
    transform_version: 1.0-powerlab-secure
    synced_at: '2026-05-19T02:27:01Z'
  icon:
    file: icon.png
    license: nominative-fair-use
    attribution: Adminer (logo trademark; sourced via CasaOS AppStore)
  security:
    image_pin: REQUIRES-DIGEST-PIN
    audit_notes: Reviewed during import from upstream catalog (see x-powerlab.source). No init scripts, no privilege escalation,
      no host-namespace bypass, no kernel capability grants. Volume paths rewritten to /DATA/PowerLabAppData/<id>.
  author: Adminer
  developer: Adminer
  main: adminer
