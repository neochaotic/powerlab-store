# HomeHub

HomeHub is a unified home automation dashboard that aggregates smart home device states from multiple backends. Displays temperature, lights, locks, and sensors in a single, mobile-friendly view.

**First steps:** Open `http://<host>:5000` and configure your smart home backends.

**Data:** `/DATA/PowerLabAppData/homehub/` — Configuration files and local state cache.
