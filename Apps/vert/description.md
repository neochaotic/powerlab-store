# VERT

Vert is a browser-based unit converter covering length, weight, temperature, volume, speed, currency, and more. Runs entirely client-side — no server requests after the initial load.

**First steps:** Open `http://<host>:80` and pick a conversion category.

**Data:** `/DATA/PowerLabAppData/vert/` — Stateless — no data stored.
