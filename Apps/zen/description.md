# Zen

Zen is a minimal, distraction-free writing environment for long-form content. Markdown editor with focus mode, word count, and export to HTML/PDF. No accounts — open and write.

**First steps:** Open `http://<host>:8080` and start writing.

**Data:** `/DATA/PowerLabAppData/zen/` — Document files stored in the configured data directory.
