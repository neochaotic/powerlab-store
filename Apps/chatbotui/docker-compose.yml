name: chatbotui
services:
  chatbot-ui:
    environment:
      PGID: $PGID
      PUID: $PUID
      TZ: $TZ
    image: ghcr.io/mckaywrigley/chatbot-ui:main
    network_mode: bridge
    ports:
    - target: 3000
      published: '3080'
      protocol: tcp
    restart: unless-stopped
version: '3.7'
x-powerlab:
  store_app_id: chatbotui
  title: ChatbotUI
  tagline: ChatbotUI
  category: System
  port_map:
  - container: 3080
    host: 3080
    protocol: http
  source:
    catalog: casaos-appstore
    upstream_id: ChatbotUI
    upstream_repo: https://github.com/IceWhaleTech/CasaOS-AppStore.git
    upstream_path: Apps/ChatbotUI/docker-compose.yml
    transform_version: 1.0-powerlab-secure
    synced_at: '2026-05-19T02:27:01Z'
  icon:
    file: icon.svg
    license: CC-BY-SA-4.0
    attribution: ChatbotUI (logo trademark; sourced via CasaOS AppStore)
  security:
    image_pin: REQUIRES-DIGEST-PIN
    audit_notes: Reviewed during import from upstream catalog (see x-powerlab.source). No init scripts, no privilege escalation,
      no host-namespace bypass, no kernel capability grants. Volume paths rewritten to /DATA/PowerLabAppData/<id>.
  author: ChatbotUI
  developer: ChatbotUI
  main: chatbot-ui
